#!/bin/bash

# $1 represents the root of the file tree

mkdir "$1"
mkdir "$1/images"
mkdir "$1/images/uploads"
mkdir "$1/images/filters"
mkdir "$1/images/outputs"
mkdir "$1/videos"
mkdir "$1/videos/uploads"
mkdir "$1/videos/outputs"
